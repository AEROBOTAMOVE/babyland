# ПЛАН — Бейби Ленд

Живо: **https://aerobotamove.github.io/babyland/**
Хранилище: `AEROBOTAMOVE/babyland`

---

## КЪДЕ СМЕ (мерено 12.08, не спомнено)

| | |
|---|---|
| записи за помощничките | **457** |
| ключове | **4685** |
| статии в библиотеката | **823** |
| живи js файла | **96** |
| инструменти в `dev/` | **34** |
| мъртви ключове | **68** от 4654 = **1.5%** (сутринта 5.3%) |
| червени на теста за телефона | **17** (сутринта 69) |

`node dev/bez_brauzar.js` → ✅ ЧИСТО — шест проверки, 7 секунди.

---

## КОЕ Е ЗАТВОРЕНО ДНЕС

**Реанимацията беше недостижима.** Картата съществуваше; „как да го съживя",
„сърдечен масаж", „реанимация", „трябва ли да му духам в устата" даваха НИЩО.
Сега 13 от 13 фрази стигат до нея, 0 фалшиви тревоги.

**Един въпрос, два отговора.** Майка със суицидна мисъл получаваше 116 111
(ДЕТСКАТА линия) на едната страница и 112 на съседната. Изравнено на 112.

**Нацепването на роудмапа.** `js/home.js` пишеше `width` и `left` на всеки
кадър от скрола, а CSS отгоре пускаше преход по същите две свойства. Сменено
с `transform`. Доказано в браузъра.

**Табът „Вход" лъжеше.** Пишеше „🔑 Вход", а отваряше профила на „Мама";
истинският вход беше недостижим. Паметта показваше „0 KB" при 802 байта.
Enter не правеше нищо. Бутон с контраст 1.18:1 в тъмна тема.

**364 кавички** отворени по български „ и затворени с права `"`.

**20-те анимации на небето**, които не спираха никъде — сега спират, когато
нещо ги покрива или табът е скрит. 20 → 0 → 20, доказано.

---

## КОЕ ОСТАВА

**Дупката „Вход".** Поправен е ТЕКСТЪТ, не дупката. Да се върне ли качването
на резервно копие вътре в самия профил — иска твоята дума.

**Пинсетната хватка.** Нова статия казва „към 18 месеца", запис `rz-hvat`
казва „между 9 и 12". Праг за насочване не се мести без педиатър.

**`plavno.js` вече вреди при скрол** (мерено: 34.2 срещу 40.3 fps без него).
Универсалният селектор `html.bl-tiho *` обезсилва ~1890 елемента два пъти на
жест. Заслужава свой пробег, преди да се маха.

**Батерията.** Направена, **недоказана**. A/B даде 8 срещу 8.

**Голите „секс" и „говор" мълчат** — съзнателно. Върнати бяха и веднага
станаха най-големите крадци в базата (20 и 10 чужди ключа).

---

## ЖЕЛЯЗНИТЕ ПРАВИЛА

**Версиите се вдигат по СЪДЪРЖАНИЕ, накрая.**
`node dev/versii_po_sadarjanie.js <последен-качен-комит>`
Часовият печат лъже: пипаш css, после index.html, и проверката „по-нов от
index.html" не намира нищо. Пипнеш ли `lib/` — вдига се и `LV` в `js/lib.js`.

**Нов файл в `js/` иска ДВА реда:** `<script>` в `index.html` **и** ред в
списъка на `sw.js`. Липсва ли вторият — не работи офлайн.

**Гол едносричен ключ не се добавя.** И многодумният не е автоматично
безопасен: „кога спи" е ПОДНИЗ на „кога спиРА".

**НИКОГА скрипт, който презаписва цял файл.** Веднъж такъв изтри базата.

---

## КАПАНИТЕ (всеки — платен веднъж)

**`\b` не важи за кирилица.** `\w` е `[A-Za-z0-9_]`. Израз с `\b` около
българска дума намира **нула** и мълчи. Три пъти за един ден.
Ползвай `(^|[^а-яa-z0-9]) … (?![а-яa-z0-9])`.

**Регекс не реже CSS.** `@keyframes` тук са на **един ред**. Брой скоби.

**`window` в Node не е глобалният обект.** `helper.js` чете голо `KB`.

**Тестът мереше кеша.** Голо `fetch` → service worker връща своето копие.
`cache: 'reload'` НЕ помага. Уникален адрес на всяко теглене.

**Скрит таб не пуска `requestAnimationFrame`** — мълчи 9.9 сек, докато
`setTimeout` гърми за 0. И: в скрит таб `document.hidden` е true, затова
мярка, която сравнява СЪСТОЯНИЕ, ще обяви правилното поведение за счупено.
Мери РАЗЛИКА, не състояние.

**Броячът се надуваше сам.** Права кавичка в КОМЕНТАР отваряше низ и текстът
на коментара се броеше за ключ: 15 фантома. Хвърляй коментарите преди броене.

**Самопроверка без изход.** Пазачът се самоизвикваше и „работеше" през
препълнен стек: 2 минути вместо 7 секунди.

**„0 счупени" ≠ „0 загуби".** Махнатият ключ изчезва от списъка.
Пускай контролни думи **на ръка**.

**Мярката не съди УМЕСТНОСТ.** Два записа с еднакъв ключ дават „0 счупени",
докато единият краде отговора на другия.

**Кодът не бива да съди по паметта си.** `if (трябва === спряноПоспомен) return;`
не забелязва, че някой е махнал класа отвън. Питай самия елемент.

**`raw.githubusercontent` кешира.** Сверявай по `api contents?ref=main`.

---

## ЗАМРАЗЕНО — не се пипа

Мед под 1 година · вода преди 6 месеца · сол и захар преди 1 година ·
38.0°+ при бебе под 3 месеца = веднага лекар · сън по гръб на твърда равна
повърхност, в празно легло · пара при круп НЕ · задавяне под 1 г. = 5 потупвания
по гърба + 5 натиска на гърдите · време по коремче САМО докато е будно и с теб ·
вода за адаптирано ~70° · **никакви дози, никога** — нито с цифри, нито с думи.

## ПРАВИЛА ЗА ВСЯКА КАРТА

Нищо, което брои какво майката НЕ е направила · нищо, което приема за
дадено партньор, пари, помощ, време, кола · празно поле не е доказателство:
„нямам записано" ≠ „няма" · мълчалив бутон няма · обещае ли текст функция —
тя трябва да съществува · факт да, обяснение защо фактът е такъв не ·
числото и думата до него не бива да си противоречат · обещание, което не важи
в твоя град, е по-лошо от никакво.

---

## ИНСТРУМЕНТИТЕ

```bash
node dev/bez_brauzar.js          # шест проверки без браузър, 7 сек
node dev/mrytvi.js [--all]       # всеки ключ намира ли своя запис
node dev/kakvo_poluchava.js "фраза" "Стая"   # целият път до отговора
node dev/opit_klyuch.js --plan X # мери щетата от МАХАНЕ, преди да пише
node dev/opit_dobavi.js --plan X # мери щетата от ДОБАВЯНЕ
node dev/metachka2.js план.json  # всеки поотделно, после всички заедно
node dev/cena_animacii.js        # кои анимации пипат подредбата
node dev/kavichki.js [--pishi]   # българските кавички
node dev/versii_po_sadarjanie.js <sha>
```

В браузъра: `dev/test_vhod.js` (входът) · `dev/test_nebe.js` (небе+балонче) ·
`dev/test_telefon.js` · `dev/test_pamet.js` · `dev/test_ezik.js` ·
`dev/test_flagove.js` · `dev/test_otgovori.js` · `dev/test_biblioteka.js` ·
`dev/vsichko.js` (всички).

Всеки от тях брои **и колко е прегледал**. „0 находки" без брой прегледани
значи „0 прегледани", не „чисто".
