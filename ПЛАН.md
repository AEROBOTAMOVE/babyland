# ПЛАН — Бейби Ленд

Живо: **https://aerobotamove.github.io/babyland/**
Хранилище: `AEROBOTAMOVE/babyland`

---

## КЪДЕ СМЕ (измерено 12.08)

| | |
|---|---|
| записи за помощничките | **457** |
| ключове | **4641** |
| статии в библиотеката | **823** |
| живи js файла | **96** |
| инструменти в `dev/` | **31** |
| мъртви ключове | **93** от 4594 = **2.0%** (сутринта 5.3%) |

`node dev/bez_brauzar.js` → ✅ ЧИСТО — шест проверки, 7 секунди.

---

## КОЕ РАБОТИ СЕГА (тече)

| фронт | какво трябва да върне |
|---|---|
| кадрите | кадри/сек при скрол, 3× със и 3× без `js/izvan_ekrana.js`; има задача **да го обори** |
| четенето | първият човек, който чете 78-те нови теми — дози, стандарти, брошурен глас |
| 93-те ключа | последните, с контролни думи на ръка след всяка партида |
| остатъкът от телефона | 18 анимации + 4 мишени + 32 жълти |
| ревизията | 7 ревизора проверяват твърденията от 27 комита срещу живия код |

---

## СЛЕД ТЯХ

**Входният екран.** `dev/test_vhod.js` дава 19 от 20. Собственикът каза, че
сме го говорили много пъти — трябва неговият списък, не моите догадки.

**Двете липсващи теми.** „Бебето е седнало" (седалищно, 32-ра седмица) и
пъпната херния — влязоха в 78-те, но не са четени.

**Батерията.** Направена, **недоказана**. A/B даде 8 срещу 8.

---

## ЖЕЛЯЗНИТЕ ПРАВИЛА

**Версиите се вдигат по СЪДЪРЖАНИЕ, накрая.**
`node dev/versii_po_sadarjanie.js <последен-качен-комит>`
Часовият печат лъже: пипаш css, после index.html, и проверката „по-нов от
index.html" не намира нищо. Пипнеш ли `lib/` — вдига се и `LV` в `js/lib.js`.

**Нов файл в `js/` иска ДВА реда:** `<script>` в `index.html` **и** ред в
списъка на `sw.js`. Липсва ли вторият — не работи офлайн.

**Гол едносричен ключ не се добавя.** Днес махнахме 19 такива, защото
крадяха от точните записи. Новото винаги е многодумно.

**НИКОГА скрипт, който презаписва цял файл.** Веднъж такъв изтри базата.
Точкови `Edit` или готовите инструменти.

---

## КАПАНИТЕ, В КОИТО ПАДНАХ (всеки — веднъж)

**`\b` не важи за кирилица.** В JavaScript `\b` се дефинира през
`\w = [A-Za-z0-9_]`. Израз с `\b` около българска дума намира **нула** и
мълчи. Три пъти за един ден. Ползвай `(^|[^а-яa-z0-9]) … (?![а-яa-z0-9])`.

**Регекс не реже CSS.** `@keyframes` тук са на **един ред**, затова
`[\s\S]*?\n\}` грабва съседните правила. Обявих 24 „скъпи" анимации —
бяха нула. Брой скоби. (`dev/cena_animacii.js`)

**`window` в Node не е глобалният обект.** `helper.js` чете голо `KB`.
Уредът каза „100% мъртви ключове" — беше счупен уред.

**Тестът мереше кеша.** Голо `fetch` → service worker връща своето копие.
`cache: 'reload'` НЕ помага. Уникален адрес на всяко теглене.

**Скрит таб не пуска `requestAnimationFrame`** — мълчи 9.9 сек, докато
`setTimeout` гърми за 0. Всеки тест с анимация виси завинаги.
(`dev/raf_podporka.js` — но НЕ я ползвай, ако мериш кадри.)

**Самопроверка без изход.** Пазачът се самоизвикваше и „работеше" през
препълнен стек: 2 минути вместо 7 секунди.

**„0 счупени" ≠ „0 загуби".** Мярката проверява само съществуващите
ключове; махнатият изчезва от списъка. Пускай контролни думи **на ръка**.

**Мярката не съди УМЕСТНОСТ.** „наддаване" в „Моето бебе" отиваше при
килограмите на майката. Технически чисто, смислово грешно.

---

## ЗАМРАЗЕНО — не се пипа

Мед под 1 година · вода преди 6 месеца · сол и захар преди 1 година ·
38.0°+ при бебе под 3 месеца = веднага лекар · сън по гръб на твърда равна
повърхност · пара при круп НЕ · задавяне под 1 г. = 5 потупвания по гърба +
5 натиска на гърдите · вода за адаптирано ~70° · **никакви дози, никога**.

## ПРАВИЛА ЗА ВСЯКА КАРТА

Нищо, което брои какво майката НЕ е направила · нищо, което приема за
дадено партньор, пари, помощ, време, кола · празно поле не е доказателство:
„нямам записано" ≠ „няма" · мълчалив бутон няма · обещае ли текст функция —
тя трябва да съществува · факт да, обяснение защо фактът е такъв не.

---

## ИНСТРУМЕНТИТЕ

```bash
node dev/bez_brauzar.js          # шест проверки без браузър, 7 сек
node dev/mrytvi.js [--all]       # всеки ключ намира ли своя запис
node dev/kakvo_poluchava.js "фраза" "Стая"   # целият път до отговора
node dev/opit_klyuch.js --plan X # мери щетата от МАХАНЕ, преди да пише
node dev/opit_dobavi.js --plan X # мери щетата от ДОБАВЯНЕ
node dev/metachka2.js план.json  # всеки поотделно, после всички заедно
node dev/cena_animacii.js        # кои анимации пипат подредбата
node dev/versii_po_sadarjanie.js <sha>
```

В браузъра: `dev/test_vhod.js` (входът) · `dev/test_telefon.js` ·
`dev/test_pamet.js` · `dev/test_ezik.js` · `dev/test_flagove.js` ·
`dev/test_otgovori.js` · `dev/test_biblioteka.js` · `dev/vsichko.js` (всички).

Всеки от тях брои **и колко е прегледал**. „0 находки" без брой прегледани
значи „0 прегледани", не „чисто".
